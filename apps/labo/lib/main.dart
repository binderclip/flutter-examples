import 'package:flutter/material.dart';
import 'package:labo/go_deep/container.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return createMyContainer();
  }
}
