import 'package:flutter/material.dart';
import 'package:fluttery/framing.dart';

void main() => runApp(new MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return RandomColorBlock();
  }
}
